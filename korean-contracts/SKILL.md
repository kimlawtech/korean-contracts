---
name: korean-contracts
description: 한국 사업자용 계약서 작성 진입점 스킬. 시작하자마자 9개 하위 스킬을 번호 메뉴로 제시하고, 번호 입력 즉시 해당 스킬 인터뷰로 직행한다.
license: Apache-2.0
version: 2.1.0
---

# Korean Contracts — 계약서 작성 허브

## 스킬 시작 시 필수 프리플라이트

**Step 0: MCP 서버 연결 확인 (메뉴 출력 직전 반드시 실행)**

아래 순서로 진행한다.

1. MCP 도구 `list_sessions` 를 호출해본다.
2. 호출이 성공하면 → 상태 문구를 **`🔒 보안 모드: MCP 서버 연결됨`** 으로 설정.
3. 호출 실패·도구 미존재 → 상태 문구를 **`⚠️  보안 모드: MCP 서버 미연결`** 으로 설정하고 아래 설치 안내를 같이 출력.

```
⚠️  보안 모드: MCP 서버 미연결 — 개인정보가 Claude 컨텍스트에 평문으로 노출됩니다.

    [1회 자동 설치 — 터미널에 그대로 복붙]

      macOS / Linux:
        bash {REPO_DIR}/install.sh

      Windows (PowerShell):
        powershell -ExecutionPolicy Bypass -File {REPO_DIR}\install.ps1

    설치 후 Claude Desktop을 완전히 종료 후 재실행하면 자동 연결됩니다.

    상세 설치 가이드: https://github.com/kimlawtech/korean-contracts#설치

    MCP 없이도 계약서 작성은 가능합니다 (플레이스홀더 모드).
    민감 정보는 생성된 파일에서 [ ] 자리에 직접 기입하세요.
```

**중요**: `{REPO_DIR}` / `{REPO_DIR}` 플레이스홀더는 `install.sh` / `install.ps1` 실행 시 실제 클론 경로로 자동 치환됩니다. 아직 치환되지 않은 상태라면 사용자에게 "이 스킬이 어느 폴더에 클론되어 있나요?"라고 물어 경로를 받은 뒤 해당 경로를 삽입해 안내합니다.

프리플라이트가 끝나면 아래 메뉴를 출력한다. 상태 문구는 배너 바로 아래 한 줄로 삽입한다.

## 스킬 시작 시 출력 (필수 — 수정 금지)

```
──────────────────────────────────────────
  SpeciAI 🇰🇷 국내 최초·최대 한국 법률 AI 허브
  👉 https://discord.gg/3gYGuMcqgb  @kimlawtech
──────────────────────────────────────────
{상태 문구 — 프리플라이트 결과}

어떤 계약서가 필요하신가요? 번호를 입력하세요.

  ── 직원 고용 ──────────────────────────
  1  근로계약서        정규직·계약직 (주 40시간)
  2  알바계약서        파트타임·단시간 (주 40시간 미만)
  3  유연근무 계약서   탄력근로·선택근로·재택근무
  4  일용직 계약서     건설·행사·단기 1일 단위

  ── 외부 계약 ──────────────────────────
  5  프리랜서 계약서   개인·1인 사업자, 3.3% 원천징수
  6  외주용역 계약서   법인 간 발주, 세금계산서

  ── 기존 계약 변경 ──────────────────────
  7  근로조건 변경     임금·근무장소·업무내용 변경
  8  연봉계약서        연봉 갱신·재협상

  ── 기타 ────────────────────────────────
  9  잘 모르겠어요     상황 설명하면 맞는 유형으로 안내

번호 또는 계약서 이름을 입력하세요.
예) "1" / "알바" / "프리랜서 계약서 만들어줘"
```

---

## 번호별 즉시 진입 규칙

번호(또는 키워드)를 받으면 **추가 질문 없이** 해당 SKILL.md를 Read 툴로 읽고
그 스킬의 [3단계] Step -1 인터뷰를 즉시 시작한다.

| 입력 | 읽을 파일 | 키워드 트리거 |
|------|-----------|--------------|
| 1 | `{REPO_DIR}/employment-contract/SKILL.md` | 근로계약, 정규직, 계약직, 직원 |
| 2 | `{REPO_DIR}/parttime-contract/SKILL.md` | 알바, 아르바이트, 파트타임, 단시간 |
| 3 | `{REPO_DIR}/flexible-contract/SKILL.md` | 유연근무, 탄력근로, 선택근로, 재택 |
| 4 | `{REPO_DIR}/daily-worker-contract/SKILL.md` | 일용직, 일당, 건설, 행사 |
| 5 | `{REPO_DIR}/freelancer-contract/SKILL.md` | 프리랜서, 3.3, 용역, 외주 개인 |
| 6 | `{REPO_DIR}/outsourcing-contract/SKILL.md` | 외주용역, 법인, 세금계산서, B2B |
| 7 | `{REPO_DIR}/contract-amendment/SKILL.md` | 변경, 합의서, 수정, 조건 변경 |
| 8 | `{REPO_DIR}/salary-renewal/SKILL.md` | 연봉, 연봉계약, 갱신, 인상 |
| 9 | — (아래 진단 가이드 실행) | 모르겠어, 잘 모름 |

**진입 즉시 출력 형식:**

```
[{스킬명} 시작합니다]
────────────────────
{해당 SKILL.md의 "스킬 시작 시 출력" 텍스트}
```

---

## 9번 선택 시 — 상황 진단

```
어떤 상황인지 말씀해 주세요.
예) "카페 직원 뽑으려고요" / "디자이너한테 일 맡기려고요" / "직원 월급 올리려고요"
```

아래 기준으로 유형 판단 후 해당 번호로 안내한다.

| 상황 | 판단 | 번호 |
|------|------|------|
| 매일 출근·지시·4대보험 | 근로계약 | 1~4 중 시간 기준으로 분기 |
| 결과물 납품·3.3% | 프리랜서 | 5 |
| 법인 간 발주·세금계산서 | 외주용역 | 6 |
| 기존 직원 조건 일부 변경 | 변경 합의서 | 7 |
| 연봉 재협상·갱신 | 연봉계약서 | 8 |

**위장 프리랜서 자동 경고:**
아래 조건이 2개 이상이면 5번 선택 전 경고 출력:
- 매일 지정 장소·시간 출근 의무
- 타사 겸업 전면 금지
- 회사 장비·도구만 사용
- 월 고정액 지급
- 발주자가 직접 업무 지시

```
⚠️  위장 프리랜서 위험이 감지됩니다.
    실질이 근로자라면 3.3%가 아닌 4대보험 가입이 맞습니다.
    잘못 처리하면 → 4대보험 소급 부과 + 퇴직금 청구 리스크
    근로계약(1번)으로 진행하시겠어요, 프리랜서(5번)로 진행하시겠어요?
```
